# shell scripts must keep LF, or a CRLF checkout fails on macOS and Linux
# with "bad interpreter" -- the pre-push hook is the one that matters
*.sh text eol=lf
.githooks/* text eol=lf
*.yml text eol=lf
