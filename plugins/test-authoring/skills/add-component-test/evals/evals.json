{
  "skill_name": "add-component-test",
  "evals": [
    {
      "id": 0,
      "prompt": "add component test for Billing: Customer creates invoice with discount",
      "expected_output": "Skill add-component-test triggers; runs Step -1 schema check, delegates to test-authoring writer agent, always spawns the verifier, summarises result.",
      "assertions": [
        {
          "text": "skill_triggered: add-component-test is selected"
        },
        {
          "text": "schema_check_executed: schema_version compared before any writes"
        },
        {
          "text": "delegation_boundary: test writing is delegated to writer agents — the orchestrator never edits test files directly"
        }
      ]
    },
    {
      "id": 1,
      "prompt": "add a Gherkin scenario for the new approval workflow",
      "expected_output": "Skill add-component-test triggers; runs Step -1 schema check, delegates to test-authoring writer agent, always spawns the verifier, summarises result.",
      "assertions": [
        {
          "text": "skill_triggered: add-component-test is selected"
        },
        {
          "text": "schema_check_executed: schema_version compared before any writes"
        },
        {
          "text": "delegation_boundary: test writing is delegated to writer agents — the orchestrator never edits test files directly"
        }
      ]
    },
    {
      "id": 2,
      "prompt": "explain how component tests are organised",
      "expected_output": "Skill add-component-test does NOT trigger. Either another skill is selected or the model answers conversationally.",
      "assertions": [
        {
          "text": "skill_not_triggered: add-component-test is NOT selected"
        }
      ]
    },
    {
      "id": 3,
      "prompt": "review the existing feature files for redundancy",
      "expected_output": "Skill add-component-test does NOT trigger. Either another skill is selected or the model answers conversationally.",
      "assertions": [
        {
          "text": "skill_not_triggered: add-component-test is NOT selected"
        }
      ]
    },
    {
      "id": 4,
      "prompt": "what does Gherkin syntax look like?",
      "expected_output": "Skill add-component-test does NOT trigger. Either another skill is selected or the model answers conversationally.",
      "assertions": [
        {
          "text": "skill_not_triggered: add-component-test is NOT selected"
        }
      ]
    }
  ]
}
