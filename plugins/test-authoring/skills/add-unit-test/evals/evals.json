{
  "skill_name": "add-unit-test",
  "evals": [
    {
      "id": 0,
      "prompt": "add unit tests for BillingService.CreateInvoice",
      "expected_output": "Skill add-unit-test triggers; runs Step -1 schema check, delegates to test-authoring writer agent, always spawns the verifier, summarises result.",
      "assertions": [
        {
          "text": "skill_triggered: add-unit-test is selected"
        },
        {
          "text": "delegation_boundary: test writing is delegated to writer agents — the orchestrator never edits test files directly"
        }
      ]
    },
    {
      "id": 1,
      "prompt": "write a unit test for the new payment validator",
      "expected_output": "Skill add-unit-test triggers; runs Step -1 schema check, delegates to test-authoring writer agent, always spawns the verifier, summarises result.",
      "assertions": [
        {
          "text": "skill_triggered: add-unit-test is selected"
        },
        {
          "text": "delegation_boundary: test writing is delegated to writer agents — the orchestrator never edits test files directly"
        }
      ]
    },
    {
      "id": 2,
      "prompt": "this unit test is failing — why?",
      "expected_output": "Skill add-unit-test does NOT trigger. Either another skill is selected or the model answers conversationally.",
      "assertions": [
        {
          "text": "skill_not_triggered: add-unit-test is NOT selected"
        }
      ]
    },
    {
      "id": 3,
      "prompt": "explain how unit tests work in this codebase",
      "expected_output": "Skill add-unit-test does NOT trigger. Either another skill is selected or the model answers conversationally.",
      "assertions": [
        {
          "text": "skill_not_triggered: add-unit-test is NOT selected"
        }
      ]
    },
    {
      "id": 4,
      "prompt": "compare NSubstitute with Moq",
      "expected_output": "Skill add-unit-test does NOT trigger. Either another skill is selected or the model answers conversationally.",
      "assertions": [
        {
          "text": "skill_not_triggered: add-unit-test is NOT selected"
        }
      ]
    }
  ]
}
